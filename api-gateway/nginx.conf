#TO DO: ainda precisa ser implementado da forma correta

events {}

http {
    server {
        listen 80;

        location /api/v1/livros {
            proxy_pass http://livros:8080;
        }
    }
}